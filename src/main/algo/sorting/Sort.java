package main.algo.sorting;

public interface Sort {

    /**
     * @param unsorted - array to sort
     * @return sorted array - non decreasing order
     */
    int[] sortNumbers(int[] unsorted);
}
